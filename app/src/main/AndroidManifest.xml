<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.petime" >

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" /> -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Petime">

        <service
            android:name=".MyIntentService"
            android:exported="false"></service>

=======
        android:theme="@style/Theme.Petime" >
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/shape2" />

        <activity
            android:name=".music"
            android:exported="true" />
>>>>>>> Stashed changes
        <activity
            android:name=".FeedbackActivity2"
            android:exported="true" />

        <activity
            android:name=".CountDownView"
            android:exported="true"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".clock"
            android:exported="true" />
        <activity android:name=".splashscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".todo" />
        <activity android:name=".study" /> <!-- <activity -->
        <!-- android:name=".ui.SettingsActivity" -->
        <!-- android:label="@string/title_activity_settings" /> -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Petime.NoActionBar" />
        <activity
            android:name=".changeimage"
            android:label="imagechange" >

            <!-- <intent-filter> -->
            <!-- <action android:name="com.example.activitytest.ACTIONSTART"/> -->
            <!-- <category android:name="android.intent.category.DEFAULT"/> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".login" />
        <activity android:name=".regist" />
        <activity android:name=".pethouse_activity"/>
    </application>

</manifest>